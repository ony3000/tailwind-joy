---
sidebar_position: 1
---

# Button

Buttons let users take actions and make choices with a single tap.

## Basics

```jsx
import { Button } from 'tailwind-joy/components';
```

The Tailwind-Joy Button behaves similarly to the native HTML `<button>`, so it wraps around the text displayed on its surface.

The demo below shows the three basic states available to the Button: default, disabled, and loading.

export function ButtonBasics() {
  return (
    <DisplayStand>
      <Button>Button</Button>
      <Button disabled>Disabled</Button>
      <Button loading>Loading</Button>
    </DisplayStand>
  );
}

<ButtonBasics />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonBasics() {
  return (
    <div>
      <Button>Button</Button>
      <Button disabled>Disabled</Button>
      <Button loading>Loading</Button>
    </div>
  );
}
```

### Disabled

Use the `disabled` prop to disable interaction and focus:

export function ButtonDisabled() {
  return (
    <DisplayStand>
      <Button disabled variant="solid">
        Solid
      </Button>
      <Button disabled variant="soft">
        Soft
      </Button>
      <Button disabled variant="outlined">
        Outlined
      </Button>
      <Button disabled variant="plain">
        Plain
      </Button>
    </DisplayStand>
  );
}

<ButtonDisabled />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonDisabled() {
  return (
    <div>
      <Button disabled variant="solid">
        Solid
      </Button>
      <Button disabled variant="soft">
        Soft
      </Button>
      <Button disabled variant="outlined">
        Outlined
      </Button>
      <Button disabled variant="plain">
        Plain
      </Button>
    </div>
  );
}
```

### Loading

Add the `loading` prop to show the Button's loading state. The Button is [disabled](#disabled) as long as it's loading. See [Loading indicator](#) and [Loading position](#) for customization options.

export function ButtonLoading() {
  return (
    <DisplayStand>
      <Button loading variant="solid">
        Solid
      </Button>
      <Button loading variant="soft">
        Soft
      </Button>
      <Button loading variant="outlined">
        Outlined
      </Button>
      <Button loading variant="plain">
        Plain
      </Button>
    </DisplayStand>
  );
}

<ButtonLoading />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonLoading() {
  return (
    <div>
      <Button loading variant="solid">
        Solid
      </Button>
      <Button loading variant="soft">
        Soft
      </Button>
      <Button loading variant="outlined">
        Outlined
      </Button>
      <Button loading variant="plain">
        Plain
      </Button>
    </div>
  );
}
```

## Customization

### Variants

The Button component supports four variants: `solid` (default), `soft`, `outlined`, and `plain`.

export function ButtonVariants() {
  return (
    <DisplayStand>
      <Button variant="solid">Solid</Button>
      <Button variant="soft">Soft</Button>
      <Button variant="outlined">Outlined</Button>
      <Button variant="plain">Plain</Button>
    </DisplayStand>
  );
}

<ButtonVariants />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonVariants() {
  return (
    <div>
      <Button variant="solid">Solid</Button>
      <Button variant="soft">Soft</Button>
      <Button variant="outlined">Outlined</Button>
      <Button variant="plain">Plain</Button>
    </div>
  );
}
```

### Sizes

The Button component supports three sizes: `sm`, `md` (default), and `lg`.

export function ButtonSizes() {
  return (
    <DisplayStand>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </DisplayStand>
  );
}

<ButtonSizes />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonSizes() {
  return (
    <div>
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </div>
  );
}
```

### Colors

The Button component supports five colors: `primary` (default), `neutral`, `danger`, `success`, and `warning`.

export function ButtonColors() {
  return (
    <DisplayStand>
      <Button color="primary">Primary</Button>
      <Button color="neutral">Neutral</Button>
      <Button color="danger">Danger</Button>
      <Button color="success">Success</Button>
      <Button color="warning">Warning</Button>
    </DisplayStand>
  );
}

<ButtonColors />

```jsx
import { Button } from 'tailwind-joy/components';

export function ButtonColors() {
  return (
    <div>
      <Button color="primary">Primary</Button>
      <Button color="neutral">Neutral</Button>
      <Button color="danger">Danger</Button>
      <Button color="success">Success</Button>
      <Button color="warning">Warning</Button>
    </div>
  );
}
```

### Decorators

### Loading indicator

### Loading position

### Link Button

### File upload

## Icon Button

## Anatomy

## API
