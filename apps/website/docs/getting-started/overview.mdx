---
sidebar_position: 1
---

import { Button as JoyButton } from '@mui/joy';
import { CssVarsProvider } from '@mui/joy/styles';
import { Button as TJButton } from 'tailwind-joy/components';

# Overview

## Introduction

Tailwind-Joy is an open-source React component library that styles the components of Joy UI (an alternative to Material UI) in the Tailwind CSS way.

## Why use Tailwind-Joy

Visually equivalent to Joy UI (except for the `font-family`[^1]), but does not depend on Emotion. Of course, it does not depend on styled-components either.

[^1]: Tailwind-Joy does not use any particular font as its default font. The default font can be selected to suit the end user's preference.

For example, comparing the button components:

export function ComponentComparison() {
  return (
    <DisplayStand className="mb-5 grid grid-cols-1 gap-4 border-none p-0 min-[375px]:grid-cols-2 dark:bg-transparent">
      <DisplayStand className="grid items-start justify-start gap-y-0">
        <CssVarsProvider modeStorageKey="theme">
          <h3>Joy UI</h3>
          <div>
            <JoyButton color="primary" size="md" variant="solid">
              Button
            </JoyButton>
          </div>
        </CssVarsProvider>
      </DisplayStand>
      <DisplayStand className="grid items-start justify-start gap-y-0">
        <h3>Tailwind-Joy</h3>
        <div>
          <TJButton color="primary" size="md" variant="solid">
            Button
          </TJButton>
        </div>
      </DisplayStand>
    </DisplayStand>
  );
}

<ComponentComparison />
